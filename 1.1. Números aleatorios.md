# Números Aleatorios

## Introducción

Un **número aleatorio** es aquel que se obtiene sin seguir un patrón predecible. En informática, los números aleatorios no suelen ser verdaderamente aleatorios, sino **pseudoaleatorios**, ya que se generan mediante **algoritmos deterministas** que simulan la aleatoriedad.

Los **números pseudoaleatorios** se obtienen a partir de una **semilla** (seed), un valor inicial que determina la secuencia generada. Si se usa la misma semilla, la secuencia será idéntica; si la semilla cambia (por ejemplo, con el tiempo del sistema), la secuencia también variará.

---

## Generación de Números Aleatorios

Dependiendo del lenguaje de programación o del entorno, existen funciones o comandos específicos para generar estos valores.

Algunos ejemplos:
- En **Bash**, se puede usar la variable interna `$RANDOM`.
- En **PHP**, existen funciones como `rand()`, `mt_rand()` o el generador más moderno `random_int()`.

---

## 🧩 Ejemplo 1: Números Aleatorios en Bash

Bash incluye una variable interna llamada `$RANDOM`, que genera un número entero entre `0` y `32767` cada vez que se invoca.

```bash
#!/bin/bash
# Ejemplo: Generar números aleatorios en Bash

echo "Generando 5 números aleatorios:"
for i in {1..5}; do
    echo $RANDOM
done

# Si se desea limitar el rango, se puede usar el operador módulo (%)
echo
echo "Número aleatorio entre 1 y 100:"
echo $((1 + RANDOM % 100))
```

**Explicación:**
- `$RANDOM` genera un entero aleatorio.
- El operador `%` limita el rango.
- `1 + RANDOM % 100` produce un número entre **1 y 100**.

---

## 🧩 Ejemplo 2: Números Aleatorios en PHP

PHP ofrece varias funciones para generar números aleatorios. A continuación se usa `random_int()`, que es **criptográficamente seguro**.

```php
<?php
// Ejemplo: Generar números aleatorios en PHP

echo "Generando 5 números aleatorios:\n";
for ($i = 0; $i < 5; $i++) {
    echo random_int(0, 32767) . "\n";
}

// Generar un número aleatorio entre 1 y 100
echo "\nNúmero aleatorio entre 1 y 100:\n";
echo random_int(1, 100);
?>
```

**Explicación:**
- `random_int(min, max)` devuelve un número entero aleatorio dentro del rango especificado.
- Es preferible a `rand()` o `mt_rand()` cuando se requiere mayor seguridad o imprevisibilidad (por ejemplo, en contraseñas o tokens).

---

## Conclusión

Los números aleatorios son fundamentales en múltiples ámbitos:
- Simulaciones y modelos estadísticos.  
- Juegos y entretenimiento digital.  
- Seguridad informática (por ejemplo, generación de contraseñas y claves).  

En programación, comprender su naturaleza y las funciones disponibles en cada lenguaje permite generar resultados imprevisibles y seguros.
